<UserControl x:Class="Statistics.StatisticGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <ToolBar>
                <ComboBox ItemsSource="{Binding QueryItems}" SelectedItem="{Binding SelectedQuery}" Margin="0" Width="111" VerticalAlignment="Center"/>
            </ToolBar>
            <DataGrid  IsReadOnly="True" AutoGenerateColumns="True" ItemsSource="{Binding Statistic}" />
        </StackPanel>
    </Grid>
</UserControl>
