<Window x:Class="Statistics.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Statistics"
        mc:Ignorable="d"
        Title="Статистика" Height="450" Width="800">
    <Grid>
        <TabControl ItemsSource="{Binding StatisticTabs}">
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <local:StatisticGrid/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
        
        <!--<TabItem Header="Статистика по регионам">
                <TabItem.Content>
                    <local:StatisticGrid DataContext="{Binding StatisticByCountries}"/>
                </TabItem.Content>
            </TabItem>
            <TabItem Header="Статистика по странам">
                <TabItem.Content>
                    <local:StatisticGrid DataContext="{Binding StatisticByRegions}"/>
                </TabItem.Content>
            </TabItem>
            <TabItem Header="Статистика по месяцам">
                <TabItem.Content>
                    <local:StatisticGrid DataContext="{Binding StatisticsByMonths}"/>
                </TabItem.Content>
            </TabItem>-->
    </Grid>
</Window>
